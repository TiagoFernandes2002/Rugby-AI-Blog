# Copy to `.env` in the frontend folder. Vite will load `VITE_` prefixed vars.

# Backend API URL used by the frontend during development
VITE_API_URL=http://localhost:4000

# If you host the backend elsewhere put the full URL above (including port)
